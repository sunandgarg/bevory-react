# Brand editorial batch 25 (local only)

Thirty catalogue identities received complete `BrandPublicContent` objects: Benchmark, Bertha's Revenge, Chang, Cherrapunji, Copperhead, Crafter's, Dictador, Diplomatico, DOJA, D'Yavol, Jin Jiji, KAH, KI NO BI, Nederburg, Oranjeboom, Pinaq, Proost, Pumori, Roberto Cavalli, Royal Ranthambore, Sangam, Scottish Leader, Short Story, Suntory, Takashimizu, Tamras, Rangeela, Zuccardi, Rock Paper Rum and Morpheus Whisky.

Producer and trade research links are kept in `BRAND_SOURCES_BATCH_25`, outside the public fields. The public copy follows the consumer-facing structure, snippet limits and banned-language checks. Structural checks do not certify factual accuracy, image rights, search ranking or whether writing appears human to a detector. A human editor should review the claims and pairings before publishing.

## Logos

The saved catalogue snapshot already had a logo URL for each of these 30 identities. No logo was added or replaced; the importer preserves the stored URLs. This check establishes URL presence, not successful image loading, authenticity or usage rights. The audit still lists 36 logo follow-ups across the locally authored set, including earlier batches.

## Progress and release

The read-only catalogue snapshot has 1,857 rows, which is not a deduplicated count of distinct brands. Locally authored guides total 438; 1,419 snapshot rows remain outside this set. Of the earlier guides, 198 still need full factual revalidation. The audit also flags 14 identity-review candidates and cannot verify live image assets without a separate check. Current production has not received this batch.

Focused tests: 37 passed. The wider suite passed 300 tests in 32 files, excluding the MySQL-dependent phone-OTP suite. Changed-file lint, TypeScript and the direct Vite production build passed. An initial full-suite invocation reached phone OTP and failed because its local MySQL server at `127.0.0.1:3308` was unavailable; the corrected exclusion run passed. The `pnpm` wrapper attempted a registry refresh and could not run in the network-restricted sandbox, so installed local binaries were used. The generated read-only audit and public-content export are under `docs/editorial/reports/`. No commit, push, database write or deployment was performed.
